const LoadingSpinner = () => {
  return (
    <div className="flex items-center justify-center min-h-screen bg-mainBackgroundColor">
      <img src="/images/Loading_Spinner.gif" alt="loading" className="max-w-full max-h-fit" />
    </div>
  );
};

export default LoadingSpinner;
